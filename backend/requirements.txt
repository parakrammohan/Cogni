fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0

# DB layer — asyncpg is the fastest Postgres driver in Python.
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3

# Auth. argon2-cffi is OWASP's current recommended password hash —
# memory-hard, GPU/ASIC-resistant. PyJWT for HS256 tokens.
pyjwt==2.10.0
argon2-cffi==23.1.0
