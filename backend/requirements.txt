fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0

# DB layer — asyncpg is the fastest Postgres driver in Python.
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3

# Auth. Argon2id is OWASP's currently-recommended password hash; no
# JWT library since sessions are opaque random tokens stored server-side.
argon2-cffi==23.1.0

# Server-side ML inference. ONNX runtime loads the four screening
# artifacts shipped in app/ml/artifacts/. python-multipart powers
# multipart/form-data uploads for the MRI endpoint.
onnxruntime==1.20.0
numpy>=2.0,<3.0
pillow>=10.0,<11.0
python-multipart==0.0.20
