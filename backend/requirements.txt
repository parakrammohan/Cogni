fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0

# DB layer — asyncpg is the fastest Postgres driver in Python.
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3

# Auth. Argon2id is OWASP's currently-recommended password hash; no
# JWT library since sessions are opaque random tokens stored server-side.
argon2-cffi==23.1.0

# PII column encryption (Fernet). Stores sensitive profile + contact
# fields as ciphertext at rest. Defends against accidental DB dumps;
# server has the key so it's not E2EE.
cryptography>=42,<46

# Server-side ML inference. Tabular models ship as joblib-serialised
# LightGBM / XGBoost / CatBoost estimators (CatBoost wins the
# dementia_oasis + adresso_agitation v3/v4 bake-offs); the MRI image
# classifier still rides on ONNX runtime. python-multipart powers the
# MRI upload endpoint.
onnxruntime==1.20.0
lightgbm==4.6.0
xgboost>=2.1,<4.0
catboost>=1.2,<2.0
scikit-learn>=1.5,<2.0
joblib>=1.4,<2.0
numpy>=2.0,<3.0
pillow>=10.0,<11.0
python-multipart==0.0.20
